name: Build, Test, Code Coverage, and Push Docker Image

permissions:
  contents: read
  packages: write

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Build with Maven
      - name: Build with Maven
        run: |
          cd my-project  # Navigate to the folder containing pom.xml
          mvn clean install -DskipTests

      # Step 3: Run tests with Maven
      - name: Run tests with Maven
        run: |
          cd my-project  # Navigate to the folder containing pom.xml
          mvn test

      # Step 4: Generate code coverage report using JaCoCo
      - name: Generate code coverage report
        run: |
          cd my-project  # Navigate to the folder containing pom.xml
          mvn jacoco:report

      # Step 5: Log in to GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}  # GitHub username
          password: ${{ secrets.GHCR_PAT }}  # GitHub Personal Access Token (PAT)

      # Step 6: Build Docker image
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/hello-api:latest .

      # Step 7: Push Docker image to GitHub Container Registry
      - name: Push Docker image to GHCR
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/hello-api:latest
